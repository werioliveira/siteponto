# Nixpacks (Railway/Coolify): fixa Node 22 e instala as ferramentas
# de build nativo exigidas pelo better-sqlite3 (node-gyp).
[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm run start"